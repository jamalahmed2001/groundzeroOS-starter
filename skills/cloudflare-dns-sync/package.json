{
  "name": "@skills/cloudflare-dns-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "cloudflare-dns-sync": "./bin/cloudflare-dns-sync" },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
